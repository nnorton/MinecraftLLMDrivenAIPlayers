// src/planner.js
//
// Thin wrapper for backwards compatibility.
// The implementation lives in src/planner_core/* so future changes don't require
// editing this large file.
module.exports = require("./planner_core");
